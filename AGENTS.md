# AGENTS.md

## Cursor Cloud specific instructions

This is an empty/starter repository (`C-Users-007-Documents-Kursor`). It contains no application code, no dependencies, and no services.

- **Tech stack:** None yet. The repo has only placeholder files (`.gitignore`, `LICENSE.md`, `README.md`).
- **Dependencies:** None. No `package.json`, `requirements.txt`, `Makefile`, `Dockerfile`, or similar exists.
- **Services:** None to start.
- **Lint/Test/Build:** No commands available — no code to lint, test, or build.
- **Setup:** No setup steps required. The update script is a no-op (`echo "No dependencies"`).

When code is added to the repository, update this file with relevant development environment instructions.
